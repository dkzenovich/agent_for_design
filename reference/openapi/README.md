# reference/openapi/

Актуальные контракты API (AS-IS). Используются для анализа влияния и совместимости.
- Формат OpenAPI/Swagger (yaml/json).
- Описывать версии и статус (v1 — заморожена, v2 — активная).
